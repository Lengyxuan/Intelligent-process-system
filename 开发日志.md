# 开发日志

## 2026-06-01 项目基线重命名与前端产品定位切换

### 修改内容

- 将 README 主定位改为“智匠系统｜基于 RAG 的智能工艺规划工作台”。
- 将前端运行时标题、副标题、按钮、侧栏入口、检索面板、诊断入口等用户可见文案切换为智匠系统语境。
- 在首页欢迎区新增五个产品区域：工艺需求输入区、工艺知识检索结果区、工艺方案生成区、方案评估区、专家审核区。
- 将原生成入口按钮文案改为“生成工艺方案”，输入框 placeholder 改为工艺需求示例。
- 隐藏原前端中偏默认人设的设置区域，后端仍保留兼容字段并使用默认值传参。
- 新增 `docs/ZHIJIANG_MVP_ROADMAP.md`、`docs/ZHIJIANG_BASELINE_CHECK.md`、`docs/ZHIJIANG_PR_PLAN.md`。

### 修改原因

本次改造是后续智匠系统 PR 的基础版本，只做项目外壳、文档、前端入口和命名层面的整理。核心目标是让用户进入系统时看到的是制造工艺规划工作台，而不是原产品定位。

### 带来的效果

- 用户可见入口统一到“智匠系统”。
- 前端保留现有请求能力，可继续调用原 `/api/chat` 生成链路。
- 原 RAG 检索底座、知识库接口、向量模型路径、FAISS、BM25、RRF 和诊断日志能力未删除。
- 后续可以在该基线之上拆分新增工业模式入口、工艺结构化解析、ProcessRetriever、ProcessSim、专家审核与知识库反哺等独立 PR。

### 未做事项

- 未实现 CAD 解析、图纸识别、PDF 解析、DINOv2、YOLO、OpenCV 或图像检索。
- 未实现 ProcessSim、工艺评分或专家审核闭环。
- 未提交 commit，等待人工检查后再决定提交。

## 2026-06-01｜PR 1｜分支名：feat/industrial-backend-entry

### 本次目标

新增智匠工业模式后端入口，使前端请求可以通过 mode=process_planning 进入独立工业模式分支，同时保留原 ARPM 检索底座。

### 修改文件

- 新增：`backend/core/process_entry.py`
- 修改：`backend/api/chat.py`
- 修改：`backend/web/static/js/chat.js`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 修改：`开发日志.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_entry.py`
  - 改动内容：新增 `handle_process_entry(request_data)`，返回兼容前端现有 schema 的工业模式占位响应，并记录 pending 模块状态。
  - 作用：为 `process_planning` 提供独立后端入口。
  - 为什么需要这个改动：先完成工业模式路由隔离，避免后续工艺能力直接混入原 ARPM 角色聊天链路。

- `backend/api/chat.py`
  - 改动内容：在 `/api/chat` 请求中读取 `mode`，缺省为 `role_chat`；当 `mode=process_planning` 时直接转入 `handle_process_entry`。
  - 作用：保留旧请求默认行为，同时让工业模式不进入角色配置、角色记忆、RAG 检索和原生成逻辑。
  - 为什么需要这个改动：满足智匠工业模式与原 ARPM 链路的入口隔离要求。

- `backend/web/static/js/chat.js`
  - 改动内容：前端构造 `/api/chat` 请求时增加 `mode: "process_planning"`。
  - 作用：点击“生成工艺方案”时进入工业模式入口。
  - 为什么需要这个改动：前端已经是智匠工艺方案入口，需要显式标记后端模式。

- `docs/ZHIJIANG_PR_PLAN.md`
  - 改动内容：新增 PR1 工业模式后端入口目标、验收和 pending 模块记录。
  - 作用：让后续 PR 拆分边界明确。
  - 为什么需要这个改动：避免本 PR 提前实现工艺解析、检索、评分、CAD 等后续能力。

- `docs/ZHIJIANG_BASELINE_CHECK.md`
  - 改动内容：新增 PR1 工业模式入口验收项。
  - 作用：补充 mode 分流与日志识别的验收标准。
  - 为什么需要这个改动：方便验证原 ARPM 链路和智匠工业模式入口均保持可控。

### 产品侧变化

智匠前端发起工艺方案生成时会携带 `mode=process_planning`，后端返回工业模式入口已启用的占位响应。旧调用如果不传 `mode`，仍默认进入原 ARPM `role_chat` 链路。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `python -c "import sys; sys.path.insert(0, 'backend'); import app; c=app.app.test_client(); r=c.post('/api/chat', json={'message':'请为小批量铝合金薄壁件生成数控铣削工艺路线','mode':'process_planning'}); print(r.status_code); print(r.json.get('status'), r.json.get('process_meta', {}).get('mode'), r.json.get('process_meta', {}).get('process_entry_enabled'))"`：通过，返回 200、`success`、`process_planning`、`True`。
- `pytest`：未通过。实际结果为 26 项测试中 21 通过、5 失败；失败项集中在既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于本 PR 修改范围。

### 风险与未完成项

- 本 PR 仅完成工业模式入口隔离。
- 未实现工艺需求结构化解析。
- 未实现工艺查询增强。
- 未实现 ProcessRetriever、ProcessSim、方案评分、专家审核闭环。
- 未实现 CAD / PDF / 图像处理能力。

## 2026-06-01｜PR 2｜分支名：feat/process-requirement-parser

### 本次目标

新增工艺需求结构化解析模块，使智匠工业模式能够从用户自然语言中提取材料、设备、批量、质量、工艺类型等结构化字段。

### 修改文件

- 新增：`backend/core/process_parser.py`
- 新增：`tests/test_process_parser.py`
- 修改：`backend/core/process_entry.py`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 修改：`开发日志.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_parser.py`
  - 改动内容：新增 `parse_process_requirement(raw_message)`，基于关键词和正则解析材料、批量、设备、质量、工艺类型、结构特征、成本约束和工期约束。
  - 作用：将自然语言工艺需求转换为稳定的 `requirement_vector`。
  - 为什么需要这个改动：为后续工艺查询增强、ProcessRetriever、ProcessSim 和方案生成提供结构化输入。

- `backend/core/process_entry.py`
  - 改动内容：在 `handle_process_entry` 中调用 `parse_process_requirement`，返回 `requirement_vector`、`missing_fields` 和 `requirement_parser_enabled=true`。
  - 作用：让 `process_planning` 工业模式入口具备第一版工艺需求解析能力。
  - 为什么需要这个改动：PR1 只完成入口隔离，PR2 需要在不进入原角色聊天链路的前提下增加结构化解析。

- `tests/test_process_parser.py`
  - 改动内容：新增 pytest 用例，覆盖典型输入、材料、批量、设备、质量、工艺类型、缺失字段和空输入。
  - 作用：验证规则解析模块可独立 import 且行为稳定。
  - 为什么需要这个改动：降低后续 PR 改动解析逻辑时破坏基础字段输出的风险。

- `docs/ZHIJIANG_PR_PLAN.md`
  - 改动内容：新增 PR2 目标、验收和范围边界，并将 `process_parser` 标记为已接入。
  - 作用：明确 PR2 与后续查询增强、检索、评分、生成能力的边界。
  - 为什么需要这个改动：避免本 PR 提前实现后续复杂能力。

- `docs/ZHIJIANG_BASELINE_CHECK.md`
  - 改动内容：新增 PR2 工艺需求结构化解析验收项。
  - 作用：补充 `requirement_vector`、`missing_fields` 和日志字段的验收标准。
  - 为什么需要这个改动：让产品验收能直接检查解析能力是否接入工业模式入口。

### 产品侧变化

用户在智匠工业模式提交工艺需求后，系统会先返回占位响应，同时在响应元数据中给出结构化解析结果。缺失的材料、设备、成本、工期等字段不会导致报错，而是以 `unknown` 标记并进入 `missing_fields`。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全部通过。
- `python -c "import sys; sys.path.insert(0, 'backend'); import app; c=app.app.test_client(); msg='请为小批量铝合金薄壁件生成数控铣削工艺路线，要求高精度，设备为三轴数控铣床。'; r=c.post('/api/chat', json={'message':msg,'mode':'process_planning'}); data=r.json; rv=data.get('process_meta', {}).get('requirement_vector', {}); print(r.status_code, data.get('status')); print(rv.get('material'), rv.get('batch'), rv.get('feature'), rv.get('process_type'), rv.get('quality'), rv.get('equipment')); print(data.get('process_meta', {}).get('requirement_parser_enabled'), rv.get('missing_fields'))"`：通过，返回 200、`success`，并解析出 `铝合金`、`小批量`、`薄壁件`、`数控铣削`、`高精度`、`三轴数控铣床`。
- `pytest`：未通过。实际结果为 34 项测试中 29 通过、5 失败；失败项仍为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于本 PR 修改范围。

### 风险与未完成项

- 本 PR 使用规则/关键词方式完成第一版解析，复杂语义和歧义场景后续可引入 LLM 解析兜底。
- 未实现工艺查询增强。
- 未实现 ProcessRetriever、ProcessSim、工艺方案生成和方案评分。
- 未实现文件格式初判、图纸/CAD/PDF/图像处理。

## 2026-06-01｜PR 3｜分支名：feat/process-query-enhancer

### 本次目标

新增工艺查询增强模块，将 PR2 解析出的结构化工艺字段拼接成面向工艺知识检索的增强查询 process_query。

### 修改文件

- 新增：`backend/core/process_query_enhancer.py`
- 新增：`tests/test_process_query_enhancer.py`
- 修改：`backend/core/process_entry.py`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 修改：`开发日志.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_query_enhancer.py`
  - 改动内容：新增 `build_process_query(raw_query, requirement_vector)`，输出 `raw_query`、`process_query`、`query_tags` 和 `missing_fields`。
  - 作用：把材料、批量、结构特征、设备、质量、工艺类型等字段转换为检索友好的标签化查询。
  - 为什么需要这个改动：后续 ProcessRetriever 需要稳定、可观察、面向工艺知识检索的查询输入。

- `backend/core/process_entry.py`
  - 改动内容：在 `handle_process_entry` 中调用 `build_process_query`，返回 `enhanced_query`、`process_query`、`query_tags` 和 `process_query_enhancer_enabled=true`。
  - 作用：让 `process_planning` 工业模式入口在结构化解析后生成增强查询。
  - 为什么需要这个改动：PR2 只有字段抽取，PR3 需要把字段组织成后续检索模块可用的查询表达。

- `tests/test_process_query_enhancer.py`
  - 改动内容：新增 pytest 用例，覆盖完整字段拼接、标签检查、unknown 处理、空输入、raw_query 保留和旧角色提示词排除。
  - 作用：验证查询增强模块可独立 import 且输出稳定。
  - 为什么需要这个改动：避免后续检索模块接入时破坏 process_query 格式。

- `docs/ZHIJIANG_PR_PLAN.md`
  - 改动内容：新增 PR3 目标、设计说明和验收，并将 `process_query_enhancer` 标记为已接入。
  - 作用：明确 PR3 与后续 ProcessRetriever、ProcessSim、评分和生成能力的边界。
  - 为什么需要这个改动：避免本 PR 提前实现后续复杂能力。

- `docs/ZHIJIANG_BASELINE_CHECK.md`
  - 改动内容：新增 PR3 工艺查询增强验收项。
  - 作用：补充 `enhanced_query`、`process_query`、`query_tags` 和 unknown 标签的验收标准。
  - 为什么需要这个改动：让产品验收能直接检查增强查询是否接入工业模式入口。

### 产品侧变化

用户在智匠工业模式提交工艺需求后，系统会先解析 `requirement_vector`，再生成面向工艺知识检索的 `process_query`。该查询保留 unknown 标签，便于后续观察哪些字段缺失；当前仍不调用真实检索或生成工艺方案。

### 本地验证引导

- 在项目根目录执行 `python -m compileall -q backend`，验证后端 Python 文件语法。
- 执行 `node --check backend/web/static/js/chat.js`，验证前端脚本语法未被破坏。
- 执行 `pytest tests/test_process_parser.py`，验证 PR2 解析能力仍可用。
- 执行 `pytest tests/test_process_query_enhancer.py`，验证 PR3 查询增强能力。
- 输入样例 1：`请为小批量铝合金薄壁件生成数控铣削工艺路线，要求高精度，设备为三轴数控铣床。`
  - 看 `requirement_vector.material=铝合金`、`batch=小批量`、`feature=薄壁件`、`equipment=三轴数控铣床`、`quality=高精度`、`process_type=数控铣削`。
  - 看 `enhanced_query.process_query` 是否包含 `[材料=铝合金]`、`[批量=小批量]`、`[设备资源=三轴数控铣床]`、`[任务=工艺规划]`。
- 输入样例 2：`请为一个不锈钢零件生成加工方案。`
  - 看 `requirement_vector.material=不锈钢`。
  - 看无法识别字段是否进入 `missing_fields`。
  - 看 `enhanced_query.process_query` 是否仍正常生成并包含 unknown 标签。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全部通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全部通过。
- 输入样例 1 的 `/api/chat mode=process_planning` smoke test：通过，返回 200、`success`、`process_query_enhancer_enabled=True`，并生成 `[行业=机械加工]`、`[任务=工艺规划]`、`[材料=铝合金]`、`[批量=小批量]`、`[结构特征=薄壁件]`、`[设备资源=三轴数控铣床]`、`[质量要求=高精度]`、`[工艺类型=数控铣削]`。
- 输入样例 2 的 `/api/chat mode=process_planning` smoke test：通过，返回 200、`success`、`material=不锈钢`，缺失字段进入 `missing_fields`，并生成包含 unknown 标签的 `process_query`。
- `pytest`：未通过。实际结果为 40 项测试中 35 通过、5 失败；失败项仍为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于本 PR 修改范围。

### 风险与未完成项

- 本 PR 只完成工艺查询增强，不调用真实检索。
- 未实现 ProcessRetriever。
- 未实现 ProcessSim、CaseQuality、FreshQuality。
- 未实现工艺方案生成、方案评分、专家审核、文件格式初判和图纸/CAD/PDF/图像处理。

## 2026-06-01｜PR 4｜分支名：feat/process-knowledge-schema

### 本次目标

新增工艺案例元数据结构和本地存储能力，将普通文本案例升级为可计算的工艺案例，为后续 ProcessRetriever、ProcessSim、CaseQuality 和 FreshQuality 提供数据基础。

### 修改文件

- 新增：`backend/storage/process_schema.py`
- 新增：`backend/storage/process_case_store.py`
- 新增：`tests/test_process_schema.py`
- 新增：`tests/test_process_case_store.py`
- 修改：`backend/core/process_entry.py`
- 修改：`开发日志.md`
- 删除：无

### 核心代码改动与作用

- `backend/storage/process_schema.py`
  - 改动内容：新增 `create_process_case` / `validate_process_case`，统一生成工艺案例字段，固定 `source_type=process_case`，为缺失字符串字段填充 `unknown`，为分数字段设置默认值并裁剪到 0~1。
  - 作用：把知识库工艺案例从普通文本升级为具备材料、结构类型、工艺类型、设备、质量、批量、公差、粗糙度和质量评分字段的结构化数据。
  - 为什么需要这个改动：后续 ProcessRetriever、ProcessSim、CaseQuality 和 FreshQuality 需要稳定、可计算的案例元数据输入。
- `backend/storage/process_case_store.py`
  - 改动内容：新增 `ProcessCaseStore`，提供 `add_case`、`get_case`、`list_cases`、`update_case`、`delete_case`，默认存储到 `runtime/arpm-app/data/process_cases.json`。
  - 作用：提供工艺案例本地 JSON 存储能力，文件不存在时自动创建，空文件或损坏文件读取时返回空列表并记录错误。
  - 为什么需要这个改动：PR4 需要先具备本地结构化案例保存能力，但不能提前接入 FAISS、embedding、真实检索或评分。
- `tests/test_process_schema.py`
  - 改动内容：新增完整案例、缺失字段默认值、分数字段裁剪、`source_type` 固定和正文保留测试。
  - 作用：验证 schema 行为稳定，缺失或异常输入不会导致系统崩溃。
  - 为什么需要这个改动：后续 PR 接入检索和评分前，需要先锁定案例结构的基础契约。
- `tests/test_process_case_store.py`
  - 改动内容：新增 store 的 add/list/get/update/delete、空文件、损坏文件和包装结构读取测试。
  - 作用：验证本地 JSON 存储的基础 CRUD 能力和容错能力。
  - 为什么需要这个改动：避免后续 ProcessRetriever 接入时因为本地案例文件异常导致流程中断。
- `backend/core/process_entry.py`
  - 改动内容：仅在 `PENDING_MODULES` 中新增 `process_knowledge_schema: available`。
  - 作用：让 `process_planning` 元数据能表达 PR4 的 schema/store 基础能力已具备。
  - 为什么需要这个改动：本 PR 不要求调用案例库，只需要保留后续模块状态标记。

### 产品侧变化

系统新增结构化工艺案例的本地保存能力。工艺案例可以带材料、结构类型、工艺类型、设备、公差、粗糙度、专家评分、成功率、使用频次、返工率等字段；缺失字段会自动补默认值，异常分数会被裁剪到 0~1。当前没有新增前端入口，也没有新增 API；`role_chat` 默认链路和 `process_planning` 现有 parser / query enhancer 输出不变。

### 本地验证引导

- 执行 `python -m compileall -q backend`，验证后端 Python 文件语法。
- 执行 `node --check backend/web/static/js/chat.js`，验证前端脚本语法未被破坏。
- 执行 `pytest tests/test_process_parser.py`，验证 PR2 工艺需求解析仍可用。
- 执行 `pytest tests/test_process_query_enhancer.py`，验证 PR3 工艺查询增强仍可用。
- 执行 `pytest tests/test_process_schema.py`，验证工艺案例 schema 默认值、固定 `source_type` 和分数裁剪。
- 执行 `pytest tests/test_process_case_store.py`，验证本地 JSON store 的 add/list/get/update/delete 和容错。

案例 1：完整工艺案例

```json
{
  "title": "铝合金薄壁件 CNC 铣削案例",
  "material": "6061铝合金",
  "structure_type": "薄壁件",
  "process_type": "CNC铣削",
  "equipment": "三轴数控铣床",
  "quality": "高精度",
  "batch": "小批量",
  "tolerance": "IT7",
  "surface_roughness": "Ra1.6",
  "expert_score": 0.9,
  "success_rate": 0.85,
  "usage_frequency": 0.7,
  "rework_rate": 0.1,
  "text": "采用粗铣、半精铣、精铣的路线，注意薄壁变形控制。"
}
```

应看到：`source_type=process_case`、`material=6061铝合金`、`equipment=三轴数控铣床`、`expert_score=0.9`、`success_rate=0.85`，案例能保存并通过 `case_id` 读取。

案例 2：缺失字段 / 异常分数案例

```json
{
  "title": "不完整工艺案例",
  "material": "45钢",
  "expert_score": 2,
  "rework_rate": -0.5,
  "text": "这是一个字段不完整的测试案例。"
}
```

应看到：缺失字符串字段填充 `unknown`，`expert_score` 被裁剪为 `1.0`，`rework_rate` 被裁剪为 `0.0`，系统不报错，案例仍能保存。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全通过。
- `pytest tests/test_process_schema.py`：通过，4 项全通过。
- `pytest tests/test_process_case_store.py`：通过，8 项全通过。
- `pytest`：未全量通过。实际结果为 52 项测试中 47 项通过、5 项失败；失败项为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于本 PR 修改范围。

### 风险与未完成项

- 本 PR 只完成工艺案例结构和本地存储。
- 未新增 API，避免扩大 app 路由注册和前端调用范围。
- 未接入 FAISS、BM25、RRF、embedding 或真实工艺检索。
- 未实现 ProcessRetriever、ProcessSim、CaseQuality、FreshQuality。
- 未实现工艺方案生成、方案评分、专家审核、知识库反哺和 CAD/PDF/PNG 文件识别。

## 2026-06-01｜PR 5｜分支名：feat/process-retriever-adapter

### 本次目标

新增 ProcessRetriever 工艺案例召回适配器，复用或适配原 ARPM 文本检索能力，从结构化工艺案例库中返回 Top-K 候选案例，为后续 ProcessSim 和方案生成提供候选集。

### 修改文件

- 新增：`backend/core/process_retriever.py`
- 新增：`tests/test_process_retriever.py`
- 修改：`backend/core/process_entry.py`
- 修改：`开发日志.md`
- 修改：`docs/ZHIJIANG_MVP_ROADMAP.md`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_retriever.py`
  - 改动内容：新增 `ProcessRetriever`，从 `ProcessCaseStore` 读取结构化工艺案例，基于字段命中和本地 BM25 fallback 计算 `keyword_score`，返回 Top-K 候选案例。
  - 作用：让 `process_planning` 工业模式具备候选工艺案例召回能力。
  - 为什么需要这个改动：PR5 需要为后续 ProcessSim、CaseQuality、FreshQuality 和工艺方案生成提供候选集，但不能提前实现评分和生成。
- `tests/test_process_retriever.py`
  - 改动内容：新增空案例库、单案例召回、多案例排序、top_k、空查询、返回结构和不返回 `process_sim` 字段测试。
  - 作用：锁定 ProcessRetriever 的基础行为和 PR5/PR6 边界。
  - 为什么需要这个改动：避免后续 PR 接入 ProcessSim 时混淆当前的字段命中召回和工艺相似度评分。
- `backend/core/process_entry.py`
  - 改动内容：在 PR2 parser 和 PR3 query enhancer 之后调用 `ProcessRetriever`，返回 `process_retrieval`，并标记 `process_retriever_enabled=true`。
  - 作用：让 `mode=process_planning` 请求能够返回候选工艺案例召回结果。
  - 为什么需要这个改动：工业模式入口需要串联需求解析、查询增强和候选案例召回三个阶段。

### 产品侧变化

`process_planning` 请求现在除 `requirement_vector` 和 `process_query` 外，还会返回 `process_retrieval`。当本地 `process_cases.json` 为空时，系统返回 `results=[]` 和 `note=no process cases available`，不会报错。当存在工艺案例时，系统会按材料、设备、工艺类型、批量、质量、结构特征等字段命中和 BM25 fallback 分数返回 Top-K 候选案例。

### 本地验证引导

运行：

```bash
python -m compileall -q backend
node --check backend/web/static/js/chat.js
pytest tests/test_process_parser.py
pytest tests/test_process_query_enhancer.py
pytest tests/test_process_schema.py
pytest tests/test_process_case_store.py
pytest tests/test_process_retriever.py
```

产品验证案例：

案例 1：铝合金薄壁件 CNC 铣削案例，材料 `6061铝合金`，结构 `薄壁件`，设备 `三轴数控铣床`，质量 `高精度`，批量 `小批量`。

案例 2：不锈钢轴类件车削案例，材料 `不锈钢`，结构 `轴类件`，设备 `数控车床`，质量 `一般精度`，批量 `中批量`。

查询输入：`请为小批量铝合金薄壁件生成数控铣削工艺路线，要求高精度，设备为三轴数控铣床。`

应看到：铝合金薄壁件 CNC 铣削案例排名高于不锈钢轴类件车削案例；返回字段包含 `case_id`、`title`、`score`、`keyword_score`、`matched_fields`、`text_preview`；`retrieval_status.process_sim=pending`。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全通过。
- `pytest tests/test_process_schema.py`：通过，4 项全通过。
- `pytest tests/test_process_case_store.py`：通过，8 项全通过。
- `pytest tests/test_process_retriever.py`：通过，7 项全通过。
- `pytest`：未全量通过。实际结果为 59 项测试中 54 项通过、5 项失败；失败项为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于 PR5 修改范围。

### 风险与未完成项

- 当前使用 fallback 关键词召回。
- 原因：原 ARPM `Retriever` / `vector_store` 强依赖全局知识库 chunk、FAISS 索引和模型加载，直接复用会牵动 role_chat 默认检索底座；PR5 范围要求适配层，不大改原检索器。
- 后续接入方式：PR5 已保留 `semantic_score`、`retrieval_status.text_vector`、`retrieval_status.bm25` 字段，后续可为 process cases 单独建立 text2vec/FAISS/BM25 索引，或在 ProcessRetriever 内增加独立向量索引适配器。
- 未实现 ProcessSim、CaseQuality、FreshQuality。
- 未实现 RRF、工艺方案生成、方案评分、专家审核、知识库反哺和 CAD/PDF/PNG 文件识别。

## 2026-06-01｜PR 6｜分支名：feat/process-sim-quality-score

### 本次目标

新增 ProcessSim、CaseQuality、TimeDecay 和 FreshQuality 评分能力，对 PR5 召回的候选工艺案例进行二次打分和重排，使系统能够区分“文本相关”和“工艺条件真正匹配”。

### 修改文件

- 新增：`backend/core/process_scorer.py`
- 新增：`tests/test_process_scorer.py`
- 修改：`backend/core/process_retriever.py`
- 修改：`backend/core/process_entry.py`
- 修改：`tests/test_process_retriever.py`
- 修改：`开发日志.md`
- 修改：`docs/ZHIJIANG_MVP_ROADMAP.md`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_scorer.py`
  - 改动内容：新增 `ProcessScorer`，实现 `process_sim`工艺相似度、`case_quality`案例质量、`time_decay`时间衰减、`fresh_quality`时间质量补偿分 和 `final_score`最终排序分。
  - 作用：对 PR5 候选案例进行工艺条件匹配度和案例可靠性评分。
  - 为什么需要这个改动：只靠文本召回无法区分“文本相关”与“工艺条件真正适合”，PR6 需要提供可观测的二次排序信号。
- `backend/core/process_retriever.py`
  - 改动内容：在字段命中 + BM25 fallback 召回后调用 `ProcessScorer`，按 `final_score` 重新排序，并在结果中返回评分明细。
  - 作用：让 `process_retrieval.results` 同时具备召回分和工艺评分分。
  - 为什么需要这个改动：后续方案生成需要更可靠的候选案例排序输入。
- `backend/core/process_entry.py`
  - 改动内容：将 `process_scorer` 状态标记为 `available`。
  - 作用：让 `process_planning` 元数据反映 PR6 评分能力已接入。
  - 为什么需要这个改动：便于产品侧和调试侧观察当前工业模式能力进度。
- `tests/test_process_scorer.py`
  - 改动内容：覆盖完全匹配、包含匹配、不匹配、unknown、CaseQuality、返工率降权、TimeDecay、FreshQuality 和 final_score。
  - 作用：锁定评分公式和边界行为。
  - 为什么需要这个改动：避免后续生成/评估 PR 改动时破坏评分契约。
- `tests/test_process_retriever.py`
  - 改动内容：更新 PR5 测试预期，验证结果包含 `process_sim`、`case_quality`、`fresh_quality` 和 `final_score`，且不生成 `process_plan`。
  - 作用：验证 PR6 评分已进入召回结果，同时保持生成能力未提前实现。
  - 为什么需要这个改动：PR6 改变了 ProcessRetriever 输出结构，需要测试覆盖。

### 产品侧变化

`process_planning` 返回的 `process_retrieval.results` 现在包含 `process_sim`、`process_sim_details`、`case_quality`、`case_quality_details`、`time_decay`、`fresh_quality`、`final_score` 和 `score_breakdown`。系统会在候选案例召回后进行二次评分和重排，让工艺条件更匹配、案例质量更好的案例排在更前面；高返工率案例会被降权，高质量老案例不会被时间衰减直接压死。

### 本地验证引导

运行：

```bash
python -m compileall -q backend
node --check backend/web/static/js/chat.js
pytest tests/test_process_parser.py
pytest tests/test_process_query_enhancer.py
pytest tests/test_process_schema.py
pytest tests/test_process_case_store.py
pytest tests/test_process_retriever.py
pytest tests/test_process_scorer.py
```

产品验证案例：

案例 1：高匹配铝合金薄壁件 CNC 铣削案例，材料 `6061铝合金`，结构 `薄壁件`，工艺 `CNC铣削`，设备 `三轴数控铣床`，质量 `高精度`，批量 `小批量`，返工率 `0.05`。

案例 2：文本相关但工艺不匹配钛合金薄壁件五轴加工案例，材料 `钛合金`，设备 `五轴加工中心`。

案例 3：字段匹配但高返工率铝合金薄壁件案例，返工率 `0.8`。

查询输入：`请为小批量铝合金薄壁件生成数控铣削工艺路线，要求高精度，设备为三轴数控铣床。`

应看到：案例 1 排名最高；案例 2 的 `process_sim` 低于案例 1；案例 3 的 `case_quality` / `fresh_quality` 被返工率拉低；每条结果包含 `process_sim`、`case_quality`、`time_decay`、`fresh_quality`、`final_score`；`scoring_status.process_scorer=available`。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全通过。
- `pytest tests/test_process_schema.py`：通过，4 项全通过。
- `pytest tests/test_process_case_store.py`：通过，8 项全通过。
- `pytest tests/test_process_retriever.py`：通过，7 项全通过。
- `pytest tests/test_process_scorer.py`：通过，9 项全通过。
- `pytest`：未全量通过。实际结果为 68 项测试中 63 项通过、5 项失败；失败项为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于 PR6 修改范围。

### 风险与未完成项

- 本 PR 只完成候选案例评分与重排。
- 当前评分基于规则和字段匹配，复杂语义匹配后续可增强。
- 未实现工艺 Prompt、工艺方案生成、方案评分、专家审核、知识库反哺。
- 未实现 CAD/PDF/PNG 文件识别、图纸视图识别、DINOv2、YOLO、OpenCV。
- 未实现复杂 RRF 多路融合。

## 2026-06-01｜PR 7｜分支名：feat/process-prompt-generation

### 本次目标

新增工艺 Prompt Builder 与工艺方案生成能力，将 PR2 的结构化需求、PR3 的增强查询、PR5/PR6 的候选工艺案例和评分结果组织为可控 Prompt，并返回结构化 process_plan。

### 修改文件

- 新增：`backend/core/process_prompt_builder.py`
- 新增：`backend/core/process_generator.py`
- 新增：`tests/test_process_prompt_builder.py`
- 新增：`tests/test_process_generator.py`
- 修改：`backend/core/process_entry.py`
- 修改：`开发日志.md`
- 修改：`docs/ZHIJIANG_MVP_ROADMAP.md`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_prompt_builder.py`
  - 改动内容：新增 `ProcessPromptBuilder.build_prompt`，将原始需求、结构化需求、增强查询、Top-K 候选案例、输出 schema 和生成约束组织为 Prompt。
  - 作用：为后续真实 LLM 工艺生成提供受约束、可审核的 Prompt 输入。
  - 为什么需要这个改动：工艺方案生成必须基于证据和结构化输出，不能让模型自由发挥。
- `backend/core/process_generator.py`
  - 改动内容：新增 `ProcessGenerator.generate`，当前使用 `fallback_template` 基于需求和候选案例生成结构化 `process_plan`。
  - 作用：让 `process_planning` 能返回工艺路线、工序卡、参数建议、设备方案、检测标准、风险提示和引用案例。
  - 为什么需要这个改动：PR7 需要把 PR2~PR6 的结果转成可交付的结构化工艺方案，同时不提前做方案评分和专家审核。
- `backend/core/process_entry.py`
  - 改动内容：在 parser、query enhancer、retriever/scorer 之后接入 `ProcessGenerator`，返回 `process_prompt` 和 `process_plan`，并标记 `process_prompt_builder` / `process_generator` 为 available。
  - 作用：形成 `raw_message -> requirement_vector -> process_query -> process_retrieval -> process_plan` 的工业模式链路。
  - 为什么需要这个改动：产品侧需要在 process_planning 响应中直接拿到结构化方案。
- `tests/test_process_prompt_builder.py`
  - 改动内容：验证 Prompt 包含用户需求、结构化字段、候选案例、输出格式要求，不包含旧定位字段，空检索结果不崩溃。
  - 作用：锁定 Prompt Builder 的约束和可观测结构。
  - 为什么需要这个改动：避免后续接入 LLM 时 Prompt 漂移或混入旧 role_chat 定位。
- `tests/test_process_generator.py`
  - 改动内容：验证有候选案例和无候选案例时都能生成 process_plan，且不返回 plan_score、expert_review、knowledge_feedback。
  - 作用：锁定 PR7 生成能力和 PR8/PR9 边界。
  - 为什么需要这个改动：避免提前实现方案评分或专家审核。

### 产品侧变化

`process_planning` 响应现在新增 `process_prompt` 和 `process_plan`。`process_plan` 包含 `route`、`operation_card`、`parameter_set`、`equipment_plan`、`inspection_standard`、`risk_notes`、`reference_cases`。当存在候选案例时，方案会引用候选案例；当候选案例为空时，系统仍返回 fallback 方案，并在 `risk_notes` 中说明缺少可参考案例、可信度较低。前端本 PR 未改动，后端已返回 process_plan，专门展示可在后续 UI 优化中补充。

### 本地验证引导

运行：

```bash
python -m compileall -q backend
node --check backend/web/static/js/chat.js
pytest tests/test_process_parser.py
pytest tests/test_process_query_enhancer.py
pytest tests/test_process_schema.py
pytest tests/test_process_case_store.py
pytest tests/test_process_retriever.py
pytest tests/test_process_scorer.py
pytest tests/test_process_prompt_builder.py
pytest tests/test_process_generator.py
```

输入样例 1：`请为小批量铝合金薄壁件生成数控铣削工艺路线，要求高精度，设备为三轴数控铣床。`

应看到：`requirement_vector`、`process_query`、`process_retrieval`、`process_plan` 均正常返回；`process_plan.route`、`operation_card`、`reference_cases` 不为空；`risk_notes` 包含薄壁件变形、装夹或精度控制相关风险。

输入样例 2：`请为未知材料的复杂零件生成加工方案。`

应看到：系统不报错；`process_plan` 仍返回；`risk_notes` 说明缺少足够参考案例或关键信息不足；`reference_cases` 可以为空；不出现 `plan_score` 或 `expert_review` 字段。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全通过。
- `pytest tests/test_process_schema.py` 和 `pytest tests/test_process_case_store.py`：通过，12 项全通过。
- `pytest tests/test_process_retriever.py` 和 `pytest tests/test_process_scorer.py`：通过，16 项全通过。
- `pytest tests/test_process_prompt_builder.py` 和 `pytest tests/test_process_generator.py`：通过，8 项全通过。
- process_entry 有候选案例 smoke：通过，返回 `process_plan`，`reference_cases` 引用候选案例，未出现 `plan_score` / `expert_review`。
- process_entry 无候选案例 smoke：通过，返回 `process_plan`，`reference_cases=[]`，`risk_notes` 标注缺少参考案例。
- `pytest`：未全量通过。实际结果为 76 项测试中 71 项通过、5 项失败；失败项为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于 PR7 修改范围。

### 风险与未完成项

- 当前使用 `fallback_template`。
- 原因：原 `backend/core/generator.py` 强绑定 role_chat 的协议解析、角色验证、记忆和 API 配置；直接复用会扩大范围并可能影响默认链路。
- 后续接入方式：保留 `ProcessPromptBuilder` 的完整 Prompt 输出，后续可新增 process 专用 LLM 调用路径，只消费 `process_prompt.prompt` 并解析为同一 `process_plan` schema。
- 未实现方案评分，PR8 继续。
- 未实现专家审核与知识库反哺，后续 PR 继续。
- 未实现文件格式初判、CAD/PDF/PNG 识别、图像召回或多模态融合。

## 2026-06-01｜PR 8｜分支名：feat/process-plan-evaluator

### 本次目标

新增工艺方案质量评估模块，对 PR7 生成的 process_plan 进行可制造性、成本、工期、质量和风险维度评分，输出 process_evaluation 和 risk_flags。

### 修改文件

- 新增：`backend/core/process_evaluator.py`
- 新增：`tests/test_process_evaluator.py`
- 修改：`backend/core/process_entry.py`
- 修改：`backend/web/static/js/chat.js`
- 修改：`开发日志.md`
- 修改：`docs/ZHIJIANG_MVP_ROADMAP.md`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 删除：无

### 核心代码改动与作用

- `backend/core/process_evaluator.py`
  - 改动内容：新增 `ProcessPlanEvaluator.evaluate(process_plan, requirement_vector, process_retrieval)`，输出五类评分、`plan_score`、`risk_flags`、`score_breakdown` 和 `evaluation_status`。
  - 作用：对已生成的结构化工艺方案做规则化质量评估，不重新生成方案。
  - 为什么需要这个改动：PR7 已能生成 process_plan，PR8 需要让系统判断方案完整性、可制造性、成本/工期/质量风险和专家复核重点。

- `backend/core/process_entry.py`
  - 改动内容：在 `process_plan` 生成后调用 `ProcessPlanEvaluator`，返回顶层 `process_evaluation`，并同步写入 `process_meta` 与 `rag_context`；将 `process_evaluator` 标记为 `available`。
  - 作用：让 `mode=process_planning` 完整链路新增方案评估结果。
  - 为什么需要这个改动：产品流程需要在方案生成之后进入质量评估步骤，同时保持 `role_chat` 默认链路不调用该模块。

- `tests/test_process_evaluator.py`
  - 改动内容：新增完整方案高分、缺路线、缺工序卡、缺检测标准、高精度无检测、薄壁件、无参考案例、高返工率案例、分数范围、空方案和入口 smoke 测试。
  - 作用：锁定 PR8 规则评分和风险标记的基础契约。
  - 为什么需要这个改动：避免后续专家审核、知识库反哺或文件识别 PR 破坏当前方案评估输出。

- `backend/web/static/js/chat.js`
  - 改动内容：最小兼容展示 `process_evaluation`，在现有“方案评估与审核记录”面板展示 `plan_score`、五类评分和风险标记摘要。
  - 作用：后端返回评估结果后，前端可直接看到核心评分与风险。
  - 为什么需要这个改动：只做 PR8 必要展示，不做 UI 风格重构。

- `docs/ZHIJIANG_MVP_ROADMAP.md`
  - 改动内容：追加 PR8 同步记录。
  - 作用：说明 PR8 在 MVP 路线图中的位置。
  - 为什么需要这个改动：保持路线图与实际开发进度一致。

- `docs/ZHIJIANG_PR_PLAN.md`
  - 改动内容：追加 PR8 目标、设计说明、验收、验证结果和后续边界。
  - 作用：记录本 PR 的工程边界和验收标准。
  - 为什么需要这个改动：防止方案评估与专家审核、知识库反哺、文件识别范围混淆。

- `docs/ZHIJIANG_BASELINE_CHECK.md`
  - 改动内容：追加 PR8 基线验收清单。
  - 作用：提供本地产品验收字段和风险标记检查项。
  - 为什么需要这个改动：便于后续回归时确认 PR8 能力未退化。

### 产品侧变化

用户在 `process_planning` 模式生成工艺方案后，响应会新增 `process_evaluation`。系统会给出可制造性、成本、工期、质量、风险和综合方案评分，并标记材料未知、设备未知、工艺类型未知、缺少参考案例、薄壁变形、高精度、引用案例高返工、检测标准缺失、路线不完整、工序卡不完整等风险。专家审核、知识库反哺和文件识别仍为 pending。

### 本地验证引导

运行：

```bash
python -m compileall -q backend
node --check backend/web/static/js/chat.js
pytest tests/test_process_parser.py
pytest tests/test_process_query_enhancer.py
pytest tests/test_process_schema.py
pytest tests/test_process_case_store.py
pytest tests/test_process_retriever.py
pytest tests/test_process_scorer.py
pytest tests/test_process_prompt_builder.py
pytest tests/test_process_generator.py
pytest tests/test_process_evaluator.py
pytest
```

输入样例 1：`请为小批量铝合金薄壁件生成数控铣削工艺路线，要求高精度，设备为三轴数控铣床。`

应看到：`requirement_vector`、`process_query`、`process_retrieval`、`process_plan`、`process_evaluation` 正常返回；`process_evaluation.plan_score` 在 0~1 范围；`risk_flags` 中可能包含 `thin_wall_deformation` 或 `high_precision_risk`；`evaluation_status.process_evaluator=available`。

输入样例 2：`请为未知材料的复杂零件生成加工方案。`

应看到：系统不报错；`process_plan` 和 `process_evaluation` 仍返回；`risk_flags` 中包含 `missing_material` 或 `no_reference_cases`；`plan_score` 不为满分；`expert_review` 与 `knowledge_feedback` 仍为 pending。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全通过。
- `pytest tests/test_process_schema.py`：通过，4 项全通过。
- `pytest tests/test_process_case_store.py`：通过，8 项全通过。
- `pytest tests/test_process_retriever.py`：通过，7 项全通过。
- `pytest tests/test_process_scorer.py`：通过，9 项全通过。
- `pytest tests/test_process_prompt_builder.py`：通过，4 项全通过。
- `pytest tests/test_process_generator.py`：通过，4 项全通过。
- `pytest tests/test_process_evaluator.py`：通过，12 项全通过。
- `pytest`：未全量通过。实际结果为 88 项测试中 83 项通过、5 项失败；失败项为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于 PR8 修改范围。
- pytest 运行时出现 `.pytest_cache` 写入权限 warning，不影响测试断言结果。

### 风险与未完成项

- 本 PR 只完成工艺方案质量评估。
- 当前评分基于规则和字段完整性，复杂成本/工期模型后续可增强。
- 未实现专家审核 Pass / Modify / Reject。
- 未实现知识库反哺。
- 未实现 CAD / PDF / PNG 文件识别、图纸视图识别、DINOv2、YOLO、OpenCV、多模态图像召回或复杂 RRF 多路融合。
- 前端只做最小展示兼容，风格重构计划在 PR10 之后单独进行。

## 2026-06-01｜PR 9｜分支名：feat/expert-review-feedback

### 本次目标

新增专家审核与知识库反哺闭环，支持 Pass / Modify / Reject，并将通过或修改后的方案写回结构化工艺案例库。

### 修改文件

- 新增：`backend/storage/process_review_schema.py`
- 新增：`backend/storage/process_review_store.py`
- 新增：`backend/core/process_review.py`
- 新增：`backend/api/process_review.py`
- 新增：`tests/test_process_review_store.py`
- 新增：`tests/test_process_review.py`
- 修改：`backend/app.py`
- 修改：`backend/core/process_entry.py`
- 修改：`backend/web/static/js/chat.js`
- 修改：`开发日志.md`
- 修改：`docs/ZHIJIANG_MVP_ROADMAP.md`
- 修改：`docs/ZHIJIANG_PR_PLAN.md`
- 修改：`docs/ZHIJIANG_BASELINE_CHECK.md`
- 删除：无

### 核心代码改动与作用

- `backend/storage/process_review_schema.py`
  - 改动内容：新增审核记录 schema，规范 `review_id`、`plan_id`、`status`、`reviewer`、`process_plan`、`modified_plan`、`process_evaluation`、`feedback_case_id` 等字段。
  - 作用：为专家审核记录提供稳定结构。
  - 为什么需要这个改动：审核记录需要独立于工艺案例保存，且非法 `status` 不能静默通过。

- `backend/storage/process_review_store.py`
  - 改动内容：新增 `ProcessReviewStore`，支持 add/get/list/update/delete，默认存储到 `runtime/arpm-app/data/process_reviews.json`。
  - 作用：提供本地审核记录持久化能力。
  - 为什么需要这个改动：专家审核结果需要可查询、可追踪，且空文件或损坏文件不能导致系统崩溃。

- `backend/core/process_review.py`
  - 改动内容：新增 `ExpertReviewService.submit_review` 和 `build_case_from_plan`。
  - 作用：处理 Pass / Modify / Reject，并将通过或修改后的方案转换为 `process_case` 写回案例库。
  - 为什么需要这个改动：PR9 需要形成“专家审核 -> 结构化案例沉淀”的闭环，但不能把 reject 方案写入高权重案例库。

- `backend/api/process_review.py`
  - 改动内容：新增 `POST /api/process/review`、`GET /api/process/reviews`、`GET /api/process/reviews/<review_id>`。
  - 作用：让前端或外部调用方可以提交审核并查询审核记录。
  - 为什么需要这个改动：API 注册代价较低，不需要改动原 `/api/chat` 链路。

- `backend/app.py`
  - 改动内容：注册 `process_review_bp`。
  - 作用：启用审核 API。
  - 为什么需要这个改动：Flask 应用需要注册新 blueprint 才能暴露审核接口。

- `backend/core/process_entry.py`
  - 改动内容：将 `expert_review` 和 `knowledge_feedback` 状态标记为 `available`。
  - 作用：让 `process_planning` 元数据反映 PR9 能力可用。
  - 为什么需要这个改动：生成链路仍不自动审核，只声明独立审核闭环已接入。

- `backend/web/static/js/chat.js`
  - 改动内容：缓存最近一次 `process_planning` 结果，并在现有评估面板中最小展示 Pass / Modify / Reject 按钮，提交到 `/api/process/review`。
  - 作用：提供轻量前端审核动作入口。
  - 为什么需要这个改动：满足 PR9 最小审核动作接入，不进行前端风格重构。

- `tests/test_process_review_store.py`
  - 改动内容：覆盖审核记录 CRUD、空文件、损坏文件和非法状态。
  - 作用：验证审核记录存储稳定。
  - 为什么需要这个改动：避免审核记录文件异常影响系统运行。

- `tests/test_process_review.py`
  - 改动内容：覆盖 pass、modify、reject、非法 status、modified_plan 必填、feedback_case_id 回填、反哺案例字段、不覆盖已有案例和 API smoke。
  - 作用：验证专家审核和知识库反哺闭环。
  - 为什么需要这个改动：锁定 PR9 核心业务规则，避免后续 PR 误将 reject 入库。

### 产品侧变化

系统新增独立专家审核闭环。工艺方案生成和评估完成后，专家可以提交 Pass、Modify 或 Reject。Pass 会把原方案转成 `source=expert_review` 的工艺案例；Modify 会优先使用 `modified_plan` 入库；Reject 只保存审核记录，不写入案例库。普通 `process_planning` 请求不会自动审核或自动写库。

### 本地验证引导

运行：

```bash
python -m compileall -q backend
node --check backend/web/static/js/chat.js
pytest tests/test_process_parser.py
pytest tests/test_process_query_enhancer.py
pytest tests/test_process_schema.py
pytest tests/test_process_case_store.py
pytest tests/test_process_retriever.py
pytest tests/test_process_scorer.py
pytest tests/test_process_prompt_builder.py
pytest tests/test_process_generator.py
pytest tests/test_process_evaluator.py
pytest tests/test_process_review_store.py
pytest tests/test_process_review.py
pytest
```

审核输入 1：`status=pass`，应看到 review 保存成功，`feedback_case_id` 不为空，新 `process_case.source=expert_review`，`feedback_status=saved_to_process_case`。

审核输入 2：`status=modify`，提供 `modified_plan`，应看到使用 `modified_plan` 写入 `process_case`，`feedback_status=modified_plan_saved_to_process_case`。

审核输入 3：`status=reject`，应看到 review 保存成功，`feedback_case_id=null`，不新增 `process_case`，`feedback_status=review_record_only`。

### 验收结果

- `python -m compileall -q backend`：通过。
- `node --check backend/web/static/js/chat.js`：通过。
- `pytest tests/test_process_parser.py`：通过，8 项全通过。
- `pytest tests/test_process_query_enhancer.py`：通过，6 项全通过。
- `pytest tests/test_process_schema.py`：通过，4 项全通过。
- `pytest tests/test_process_case_store.py`：通过，8 项全通过。
- `pytest tests/test_process_retriever.py`：通过，7 项全通过。
- `pytest tests/test_process_scorer.py`：通过，9 项全通过。
- `pytest tests/test_process_prompt_builder.py`：通过，4 项全通过。
- `pytest tests/test_process_generator.py`：通过，4 项全通过。
- `pytest tests/test_process_evaluator.py`：通过，12 项全通过。
- `pytest tests/test_process_review_store.py`：通过，8 项全通过。
- `pytest tests/test_process_review.py`：通过，9 项全通过。
- process_planning smoke：通过，返回 `process_plan=True`、`process_evaluation=True`、`expert_review=available`、`knowledge_feedback=available`。
- `/api/process/review` pass smoke：通过，返回 200、`success=True`、`feedback_status=saved_to_process_case`、`feedback_case.source=expert_review`。
- `pytest`：未全量通过。实际结果为 105 项测试中 100 项通过、5 项失败；失败项为既有 `tests/test_config.py::TestAblationConfig::test_rag_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_kb_only_disabled`、`tests/test_retriever.py::TestRetrieverAblation::test_chat_only_disabled`、`tests/test_role_validator.py::TestRegexValidator::test_forbidden_pattern`、`tests/test_role_validator.py::TestRoleValidatorIntegration::test_layer_selection`，不属于 PR9 修改范围。
- pytest 运行时出现 `.pytest_cache` 写入权限 warning，不影响测试断言结果。

### 风险与未完成项

- 本 PR 只完成专家审核与知识库反哺。
- 当前反哺案例是“专家审核案例”，不是实际投产成功案例；`plan_score` 只作为专家评分默认值，不代表最终生产成功率。
- 未实现文件格式初判、CAD/PDF/PNG 文件识别、图纸视图识别、DINOv2、YOLO、OpenCV、多模态召回或复杂 RRF。
- 前端只做最小审核动作接入，未做风格重构。
- 真实 LLM 替换 `fallback_template` 不在本 PR 范围内。
